# 컴퓨터 네트워크 정리

## 3주차
### DHCP:
1. 호스트에게 동적으로 주소할당을 하기위해 사용한다.
2. 임시 IP 주소를 할당한다. 제한된 시간동안 제공 가능
3. DHCP 클라이언트-서버의 패러다임 구조이다.
4. Static address Allocation: 고정 IP주소와 물리적 주소를 보관
5. dynamic address Allocation : 동적 IP주소를 보관

DHCP 동작
- DHCP 클라이언트가 DHCP 서버에게 요청 신호를 보내면 DHCP 서버는 static database를 확인한 후
요청된 물리 주소의 항목이 있으면 고정 IP주소를 할당하고
- 요청된 물리적 주소가 없으면 주소 pool에 사용가능한 동적 IP주소를 클라이언트에게 할당 한 후 dynamic database에 저장한다.

### NAT:
1. 사설 주소와 범용주소의 매핑 기능을 제공해주는 기술을 네트워크 변환 기술이라고한다.
2. 한 사이트 내에서 내부 통신을 위해서는 사설 주소를 사용하고 다른 네트워크와 통신을 위해서는 범용 주소를 할당 받아서 사용한다.
3. 목적지가 다를경우는 1 대 다 관계를 유지하기위해 추가정보를 이용해서 변환테이블을 이용한다.

NAT의 예:
-20개의 컴퓨터만 가지고 있는 소규모회사
    20 or 25개를 내부 통신을 위해서 사설주소를 사용
    5개의 주소를 ISP에게 할당 받아서 전역주소를 사용한다
    5대의 컴퓨터만 인터넷 접속
    사설 주소와 범용주소의 매핑 기능을 제공한다.

### 목적지 주소에 기반한 포워딩
1. 포워딩은 포워딩 테이블을 갖는 호스트나 라우터가 필요
2. 호스트가 전송할 패킷이 있거나 패킷을 받은 라우터가 포워딩할 패킷이 있을때 다음 홉을 찾기위해 포워딩 테이블을 참조한다.
3. 목적지 주소가 나오면 마스크를 시켜서 목적지 주소가 나오는데로 출력 포트로 넘겨주는 것

### 주소 집합
1. 비클래스 주소 할당은 관리가능한 블록으로 나누어서 할당함으로 라우팅 테이블의 항목이 증가한다.
2. 이를 해결하기위해서 주소 집단이라는 개념을 도입
3. 네 개의 조직에 대한 주소 블록이 하나의 큰 블록으로 주소집합

### 가장 긴 마스크 매칭
가장 긴 마스크에서 가장 짧은 마스크의 순으로 정렬되어 있다는 것을 의미
라우팅 테이블이 긴 접두사부터 정렬되어 있지 않다면 /24 마스크가 적용된 후 패킷은 라우터 R1으로 잘못 전달 될 수 있다.

프리픽스가 긴 것부터 라우팅 테이블에서 엔트리를 구성해서 그것 부터 체크하고 목적지 주소가 나오고 프리픽스가 슬래쉬를 마스크해가지고 목적지를 찾는데  
프리픽스가 긴 것부터 라우텅 테이블에서 젤위에부터 엔트리를 구성해서 맞는 엔트리를 찾는것

왜? 롱기스트 마스크 매칭을 하느냐?
작은 거부터 찾으면 테이블에서 못 찾았으니까 디폴트로 간다 =>전혀 다른 곳으로 패킷을 날라가버림
작은 네트워크로 가는것을 먼저 뽑아내야됨




### IPv4 프로토콜


# 컴퓨터 네트워크 정리

## 3주차
### DHCP:
1. 호스트에게 동적으로 주소할당을 하기위해 사용한다.
2. 임시 IP 주소를 할당한다. 제한된 시간동안 제공 가능
3. DHCP 클라이언트-서버의 패러다임 구조이다.
4. Static address Allocation: 고정 IP주소와 물리적 주소를 보관
5. dynamic address Allocation : 동적 IP주소를 보관

DHCP 동작
- DHCP 클라이언트가 DHCP 서버에게 요청 신호를 보내면 DHCP 서버는 static database를 확인한 후
요청된 물리 주소의 항목이 있으면 고정 IP주소를 할당하고
- 요청된 물리적 주소가 없으면 주소 pool에 사용가능한 동적 IP주소를 클라이언트에게 할당 한 후 dynamic database에 저장한다.

### NAT:
1. 사설 주소와 범용주소의 매핑 기능을 제공해주는 기술을 네트워크 변환 기술이라고한다.
2. 한 사이트 내에서 내부 통신을 위해서는 사설 주소를 사용하고 다른 네트워크와 통신을 위해서는 범용 주소를 할당 받아서 사용한다.
3. 목적지가 다를경우는 1 대 다 관계를 유지하기위해 추가정보를 이용해서 변환테이블을 이용한다.

NAT의 예:
-20개의 컴퓨터만 가지고 있는 소규모회사
    20 or 25개를 내부 통신을 위해서 사설주소를 사용
    5개의 주소를 ISP에게 할당 받아서 전역주소를 사용한다
    5대의 컴퓨터만 인터넷 접속
    사설 주소와 범용주소의 매핑 기능을 제공한다.

### 목적지 주소에 기반한 포워딩
1. 포워딩은 포워딩 테이블을 갖는 호스트나 라우터가 필요
2. 호스트가 전송할 패킷이 있거나 패킷을 받은 라우터가 포워딩할 패킷이 있을때 다음 홉을 찾기위해 포워딩 테이블을 참조한다.
3. 목적지 주소가 나오면 마스크를 시켜서 목적지 주소가 나오는데로 출력 포트로 넘겨주는 것

### 주소 집합
1. 비클래스 주소 할당은 관리가능한 블록으로 나누어서 할당함으로 라우팅 테이블의 항목이 증가한다.
2. 이를 해결하기위해서 주소 집단이라는 개념을 도입
3. 네 개의 조직에 대한 주소 블록이 하나의 큰 블록으로 주소집합

### 가장 긴 마스크 매칭
가장 긴 마스크에서 가장 짧은 마스크의 순으로 정렬되어 있다는 것을 의미
라우팅 테이블이 긴 접두사부터 정렬되어 있지 않다면 /24 마스크가 적용된 후 패킷은 라우터 R1으로 잘못 전달 될 수 있다.

프리픽스가 긴 것부터 라우팅 테이블에서 엔트리를 구성해서 그것 부터 체크하고 목적지 주소가 나오고 프리픽스가 슬래쉬를 마스크해가지고 목적지를 찾는데  
프리픽스가 긴 것부터 라우텅 테이블에서 젤위에부터 엔트리를 구성해서 맞는 엔트리를 찾는것

왜? 롱기스트 마스크 매칭을 하느냐?
작은 거부터 찾으면 테이블에서 못 찾았으니까 디폴트로 간다 =>전혀 다른 곳으로 패킷을 날라가버림
작은 네트워크로 가는것을 먼저 뽑아내야됨

### 레이블 기반 포워딩

1. 1980년대에 라우팅을 스위칭으로 대치하면서 IP를 연결지향 프로토콜로 바꾸려는 노력이 시도되었다.(레이블 기반 패킷 포워딩)
2. 라우팅은 테이블 내용에 대한 탐색을 기반으로하고, 스위칭은 인덱스를 이용하여 테이블에 접근함으로써 수행된다.
3. 레이블이 테이블의 인덱스로 사용되기 때문에 해당정보를 신속하게 찾을 수 있다.

### MPLS
1.라우터는 목적지기반이지만 교환은 레이블 기반으로 패킷 포워딩
2. 이를 위해서 새로운 헤더 추가

### IPv4 프로토콜

1. IPV4는 네트워크 계층에서 패킷화 , 포워딩 및 패킷을 전달의 역할을한다.
2. ICMPv4는 IPv4를 도와 패킷전달시 발생 할 수 있는 오류를 제어한다.
3. IGMP는 IPv4의 멀티캐스팅을 담당하고, ARP IP주소와 MAC주소를 매핑한다.


### 단편화
1. 수신된 프레임의 형식과 크기는 그 프레임이 지나온 물리 네트워크의 프로토콜에 따라다르다.
2. 예를 들어, 라우터가 LAN에서 WAN으로 연결되어 있다면 라우터는 LAN 형식의 프레임을 받고 다시 WAN형식으로 바꾸어 전송한다.
3. 큰 크기의 프레임이 작은 크기의 프레임으로 바뀌어 전송되어야 할 경우 단편화가 수행된다.

### 단편화 관련된 필드
1. 식별자: 단편들은 같은 식별자 값을 갖는다
2. 플래그: 3비트 필드
M비트: 1이면 more fragment, 0이면 last
단편를 수행하여야하는데 d비트가 1로 설정되어있을경우 데이터그램을 폐기하고 ICMP오류 메세지 전송

3. 단편화 옵셋: 13비트필드
- 전체 데이터그램에서 단편의 상대적 위치
- 원 데이터그램 내에서 데이터의 옵셋을 8바이트 단위로 나타낸다.

### 데이터그램
1. IP에서 사용하는 패킷을 데이터그램이라고한다.
2. 데이터그램은 가변길이이고 헤더와 페이로드로 구성되어있다.
3. 헤더는 20~60 바이트이고 라우팅과 전달에 필수적인 정보가 들어있다.

### IPv4옵션
1. sigle byte
    no operation 무연산 옵션
    end of option 경로끝 옵션
    패딩에 사용
2. multiple byte
    recode route 경로 기록옵션
    경로 기록에 사용
    strict source route option 엄격한 발신지 경로 옵션
    지나갈 라우터들을 미리 정해놓은 뒤
    옵션에 지정된 라우터들을 모두 지나가야함
    loose source route option 느슨한 발신지 경로 옵션
    엄격한 발신지 경로 옵션과 비슷하지만 옵션에 지정된 라우터들을 모두 안 지나가도되고
    우회해서 가도됨
    timestamp 타임스탬프
    하나의 라우터에서 다음 라우터까지 가는데 걸리는 시간을 추정
    라우터가 데이터그램을 처리하는 시간 기록

### IPv4 보안
1. 패킷도청 -> 암호화
2. 패킷변조 -> 데이터 무결성
3. IP 위장 -> 인증

### IPsec 프로토콜
1. 알고리즘과 키의 결정
2. 패킷 암호화
3. 데이터 무결성
4. 발신지 인증

### 계층적 라우팅
- 많은 수의 호스트와 라우터가 있는 인터넷에서의 유니캐스트는 계층적 라우팅을 사용한다
- 계층적 라우팅이란 서로 다른 라우팅 알고리즘을 사용하여 몇몇 단계로 수행되는 라우팅

### 유니캐스트 라우팅 인터넷에서의 라우팅방법 아이디
- 유니캐스트 라우팅의 패킷은 포워딩 테이블의 도움을 받아 홉 단위로 라우팅된다.
- 인터넷의 네트워크들에 존재하는 라우터들만 포워딩 테이블의 도움이 필요하다
- 로컬 네트워크는 필요하지않다 -> 디폴트 라우터들이 존재하기때문에

### 최소비용라우팅
- 인터넷 네트워크가 가중치 그래프로 모델링 될때 , 발신지 라우터에서 목적지 라우터로의 경로를 정하는데
이 경로가 전체비용의 최소 비용이 되게 경로를 선택하는것이다.

### 라우팅 알고리즘
- 최소비용 표현과 최소비용 트리를 만드는 방법의 차이


### 거리 벡터 라우팅?
- 최선의 경로를 찾는것
- 각 노드가 인접한 이웃들에게 기초정보를 받아서 자신의 최소 비용 트리를 만드는 것이다.
- 불완전한 트리를 완성된 트리로 만들기 위해서 각 노드들인 인접한 노드들끼리 교환의 과정을 한다.
- 네트워크 정보가 불완전하더라도 완성된 트리를 만들기위해서 끊임없이 자신의 이웃들에게 알린다.
- 벨만 포드 알고리즘 사용

### 벨만 포드 알고리즘
- 라우터와 망을 노드로 인식하고 이것들을 연결한 링크로 구성된 그래프 취급
- 노드들 간의 거리가 주어진 망에서 발신지에서 목적지까지의 경로의 최단 거리를 구하기 위해서 벨만포드 알고리즘을 사용하는데
- 발신지에서 중계 노드까지의 비용과 중계노드에서 목적지까지의 비용이 최소비용으로 나온 상황에서
중계 노드를 거치는 발신지 노드 x와 목적지 노드 y사이의 경로에서 최소비용을 찾는다.

### 최소 비용트리
- 최소비용트리는 루트에서부터 모든 목적지까지의 최소비용 경로와 결합

### 거리 벡터
- 최소비용 경로에 대한 최소비용 트리를 나타내는 일차원 배열을 이용

### 거리 벡터 갱신
- 모든 노드들은 일차원 배열 형태의 벡터를 만든 후 인접한 노드들에게 벡터 정보를 받는다
- 벡터 정보를 받으면 노드들은 벨만 포드 방정식을 사용하여 자신의 거리벡터를 갱신한다.

### 거리벡터 라우팅의 문제점
- 무한대로의 카운트 ->두 노드의 루프 문제
- 링크 고장난 경우에 다른 모든 라우터가 이를 인식해야되는데 거리벡터 라우팅에서는 시간이 많이소요

### 거리벡터 라우팅 해결책
1. 무한대로의 정의 : 16과 같은 작은 수로 정의
2. 수평 분할: 노드 b가 x를 갈때 노드a를 거쳐가야하는게 최적 경로인걸 알았으면 a한테 x에 관한 정보를 보내지 않는다.
3. 노드 a는 x를 무한대 취급 가능하다

4. 수평분할 + poison reverse
- 거리 벡터 프로토콜은 타이머 사용, 경로 상에 패킷이 지나가지 않으면 테이블에서 이 경로를 삭제
- 정보를 보내지 않으면 수평분할 정책인지, 경로를 제거한지 알 수 없어
- 노드 b는 x로의 거리값을 무한대로 설정하고, 정보의 송신자인 a라우터에게 보낼 경우
"이 값은 당신에게 정보를 받은거다" 경고
<!--
4주차 정리
-->

## 6주차
### IPv6 주소
1. 주소 공간을 늘리기 위해
2. 128비트 or 16바이트의 공간 Ipv4 주소길이의 4배이다.

### 표현법
- 표현법: 16진수 콜론 표기법, 이진법
- 축약: IPv6 주소는 길지만 많은 수의 0을 포함
- 섹션앞의 0을 생략한다
- 0000은 0으로 표현할 수 있고 연속되는 섹션이 0으로만 구성되어있을경우
0을 모두 제거하고 더블 콜론으로 대체 할 수 있다.
- 주소당 한 부분만 가능

### IPv6 주소공간
- 2 ^128
- Ipv4에 2 ^96배 정도

### 주소 공간 할당
- IPv4 주소공간과 같이 IPv6도 다양한 크기의 몇몇 블록으로 나누어져있으며
특정 목적에 맞게 블록을 할당했다.
- 001 2000::/3 경우는 global 유니캐스트 목적이다 인터넷 1/8

### 글로벌 유니캐스트 주소 128비트
- global prefix 48비트 subfix 16비트 interface 64비트
- isp 2^45기관
- EUI-64 물리주소는 처음에 사용하던 Mapping이었고 첫번째 7번째 비트를 1로 교체
- Ethernet Mac address 첫번째 7번째 비트를 1로 교체 중간에 ff:fe를 추가함


### IPv6 자동구성
- IPv6 자동으로 dhcp에게 주소를 할당 받을수도있겠지만 호스트가 자체적으로 주소를 구성할수도있다
- 링크 로컬 어드레스
- 이웃 모집 메세지, 이웃 광고 메세지: 링크 로컬 주소를 생성 및 확인
- 라우터 모집 메세지, 라우터 광고 메세지: 글로벌 유니캐스트 주소확인, 접두사, 서브넷 접두사

### 자동 구성 구현
1. 이더넷 주소 카드를 받으면 호스트는 이더넷 주소를 이용하여 인터페이스 식별자를 생성한다.
2. 호스트는 사설 주소를 이용하여 링크 로컬 주소를 생성한다.
3. 인접한 이웃들에게 겹치는 주소가 있는지 확인하고
4. 이 주소가 유일하다고 가정하면 라우터 모집 메세지를 전송
5. 글로벌 유니캐스트 접두사와 서브넷 식별자를 결합하여 라우터 광고메세지를 수신한다.
6. 그러면 호스트는 자신의 글로벌 유니캐스트 주소를 찾아 저장하기위해
이 접두사에 자신의 인터페이스 식별자를 첨부한다.

### IPv6 개발한이유?
1. 확장된 주소공간
2. 더 나은 헤더형식(옵션분리)
3. 새로운 옵션
4. 확장 허용
5. 자원 할당 지원: 트래픽 클라스, 흐름 라벨 지원
6. 보안: 기밀성, 무결성

### IPv6 지연이유?
1. 비클래스형 주소 지정
2. DHCP
3. NAT

### 패킷 형식
1. 기본헤더 40바이트, 페이로드 65,535 바이트
2. 패킷 =헤더+페이로드
3. Next header: 첫 확정 헤더의 종류를 정의하거나 데이터그램의 기본 헤더를 뒤따르는 헤더를 정의한다.
선택적 확장헤더나 UDP/TCP 헤더
4. Hoplimit: Ipv4 ttl과 같은 목적이다

### 확장 헤더
1. Ipv6 패킷은 기본 헤더 40바이트와 확장헤더로 이루어져있는데
2. 기본헤더에 기능성을 더 추가하기위해 뒤에 6개의 확장헤더를 더 둘 수 있다.

### 확장 헤더의 종류
1. 홉간 옵션: 발신지가 데이터그램이 거쳐가는 모든 라우팅에게 정보를 보낼 필요가 있을때 사용
    - `Pay 1`: 1byte 정렬
    - `Pay N`: 2byte 이상 정렬
    - `jumbo payload`: 65,535의 페이로드가 넘었다
2. 목적지: 발신지가 특정 라우터에게 정보를 보낼 필요가 있을 때 사용
3. 발신지 라우팅: 엄격한/느슨한 발신지 옵션 합친거
4. 단편화: 
    - ipv6는 발신지에서만 단편화가능
    - 경로 탐색 기법을 사용해서 경로 상의 mtu를 찾는 방식
5. 인증: 송신자인지 판단, 데이터그램의 무결성
6. 암호화된 보안 페이로드: 데이터그램의 기밀성을 지키기 위한 기능

### 확장헤더 비교
1. '+ 인증, + 암호화된 보안 페이로드'
2. '- 타임스탬프, - 경로기록 옵션'
3. 발신지 경로 선택사항은 발신지 경로 확장헤더로 이동
4. 단편화 필드-> 단편화 확장헤더

### ICMPv6
1. ICMPv4 독립적이던 프로토콜이 ICMPv6에 포함되고 유용성을 위해서 새로운 메세지도 추가되어 더 복잡해짐
2. ICMP와 IGMP 합쳐져서 ICMPv6가 되었다


### ICMP6 메세지들
1. 오류보고메세지
    - 목적지 도달불가
    - 패킷이 너무큼
    - 시간 초과
    - 파라미터 문제
2. 정보메세지
    - echo request : 라우터나 호스트에게 보냄
    - echo reply: 라우터나 호스트가 보냄
    통신 할 수있는지 확인

3. 이웃 발견 메세지
- 호스트들은 패킷을 전송하려는 이웃 라우터들을 찾기위해서 ND 프로토콜을 사용하고
- 노드들은 네트워크 상의 이웃들의 링크 계층 주소를 찾기위해 ND프로토콜을 사용
- 노드들은 이웃 IPv6 주소들을 찾기위해 IND 프로토콜을 사용한다.


    1. 라우터 모집 메세지: 발신지가 수신지의 네트워크 상의 라우터를 찾기 위해 사용
    2. 라우터 광고 메세지: 라우터 모집메세지 응답
    3. 이웃 모집 메세지: 
     - 발신자가 수신자의 링크 계층 주소를 요청할 경우 사용
     - 이웃한 노드에 접근이 가능한지 여부를 판단하기 위해 사용
    4. 이웃 광고 메세지: 이웃 모집 메세지의 응답
    5. 재지정 메세지: IPv4의 redirection과 동일
    호스트가 타켓 라우터의 물리적 주소를 알 수 있는 기능 추가 
    6. 역이웃 모집/ 광고 메세지: 링크 계층 주소를 통해 호스트나 라우터의 IPv6 주소를 검색

4. 그룹 멤버쉽 메세지
- IPv4에 멀티캐스트를 담당하는게 IGMPv3가 있다면, IPv6는 그룹멤버쉽 메세지인 MLDv2가 있다.
- 멤버쉽 질의 메세지: 네트워크에서 동적인 그룹 멤버를 찾기위해 라우터에 의해 전송
- 멤버쉽 보고 메세지: 멤버십 질의 메세지에 대한 응답

## IPv4-> IPv6
- 시스템 사이에서 문제가 없도록 매끄럽게 진행되어야한다.
- 전략
    1. 이중 스택: 시스템에서 IPv4와 IPv6를 동시에 지원.
    2. 터널링: IPv6를 사용하는 두 호스트가 통신을 할 때 패킷이 IPv4를 사용하는 지역을 지나갈때 사용 가능한 방법
    3. 헤더 변환: IPv6로 인터넷 세상이 변했을때 일부분의 IPv4의 헤더를 IPv6호스트와 통신 할 수 있게 변환

<!-- 6주차 정리2>

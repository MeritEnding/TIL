# 데이터 베이스 정리

## 1. 2주차 학습

목차 
    
- 데이터베이스 시스템 개요
- 데이터베이스 구조
- 데이터베이스 사용자
- 데이터베이스 언어
- 데이터베이스 관리 시스템의 구성

### 데이터베이스 시스템(DBS Database System)

    데이터 베이스에 데이터를 저장하고, 이를 관리하여 조직에 필요한 정보를 생성해주는 시스템

### 데이터베이스 구조

1. 데이터 베이스 스키마
    - 전체적인 데이터베이스 구조
    - 데이버테이스에 저장되는 데이터 구조와 제약조건을 정의한 것
    - 자주 변경되지는 않는다.
    - 데이터베이스의 모든 가능한 상태를 미리정의한다.

2. 데이터베이스 인스턴스
    - 스키마에 따라 데이터베이스에 실제로 저장된 값
    - 특정 시점의 데이터베이스의 내용을 의미한다.
    - 시간이 지남에 따라 계속해서 바뀐다.

데이터베이스 스키마는 정의 느낌, 데이터베이스 인스턴스는 상태느낌

3. 3단계 데이터베이스 구조

    - 외부단계: 개별 사용자 관점
    - 개념단계: 조직 전체의 관점
    - 내부단계: 물리적인 저장 장치의 관점

    내부단계 => 외부단계로 갈수록 추상화 레벨 높아짐

    1. 외부단계
        - 데이터베이스를 개별 사용자 관점에서 이해하고 표현하는 단계
        - 데이터베이스 하나에 외부 스키마가 여러 개 존재할 수 있다.
        - 최종 사용자와 응용 프로그래머들은 데이터베이스의 일부분에만 관심을 가진다.
        - 외부 스키마: 외부단계에서 사용자에게 필요한 데이터베이스를 정의 한 것

        - 각 사용자가 생각하는 데이터베이스의 모습, 즉 논리적 구조로 사용자마다 다르다.
        - 서브 스키마라고도 한다.

    2. 개념단계
        - 데이터 베이스를 조직 전체의 관점에서 이해하고 표현하는 단계
        - 데이터베이스 하나에 개념스키마가 하나만 존재한다.
        - 물리적인 구현은 고려하지 않고 전체에 관한 스키마를 포함한다.

        - 개념 스키마: 개념 단계에서 데이터베이스 전체의 논리적 구조를 정의한 것
        - 조직 전체의 관점에서 생각하는 데이터베이스의 모습
        >> 어떤 데이터가 저장되어있고 데이터간에는 어떤 관계가 존재하고 어떤 무결성 제약조건들이 명시되어 있는가를 기술한다.

        - 데이터에 대한 보안 정책이나 접근 권한에 대한 정의도 포함한다.
    3. 내부단계
        - 데이터베이스를 저장 장치의 관점에서 이해하고 표현하는 단계
        - 데이터베이스 하나에 내부 스키마가 하나만 존재한다.
        - 내부스키마: 실제의 물리적인 데이터 구조에 관한 스키마
        >> 전체 데이터베이스가 저장 장치에 실제로 저장되는 방법을 정의한 것, 데이터베이스에 어떤 데이터가 어떻게 저장되어 있는가를 기술한다.
        - 레코드 구조,필드 크기,레코드 접근 경로 등 물리적 저장구조를 정의한다.
        - 인덱스, 해싱 등과 같은 접근 경로, 데이터 압축 등

## 3주차 학습

### 관계 데이터 모델의 개념(관계 DBMS)

- 가장 개념이 단순한 데이터 모델
- 가장 널리 사용되는 DBMS
- 사용자는 원하는 데이터 What만 명시하고
 어떻게 이 데이터를 찾을 것인가 how는 명시할 필요가없다

### 관계 데이터 모델 성공의 요인
- 간단한 테이블(릴레이션)을 사용한다
- 집합위주로 데이터를 처리한다.

### 관계 데이터 모델의 기본 개념

- 개념적 구조를 논리적 구조로 표현하는 논리적 데이터 모델 중 하나
- 하나의 개체에 대한 데이터를 하나의 릴레이션에 저장한다.

    1. 릴레이션
    : 하나의 개체에 관한 데이터를 2차원 테이블의 구조로 저장한 것
    파일 관리 시스템 관점에서 파일에 대응

    2. 속성
    : 릴레이션의 열, 애트리뷰트

    3. 투플
    : 릴레이션의 행
    
    4. 도메인
        - 하나의 속성이 가질 수 있는 모든 값의 집합
        - 속성 값을 입력 및 수정할 때 적합성 판단의 기준이 된다.
        - 일반적으로 속성의 특성을 고려한 데이터 타입으로 정의  

    5. 널
        - 알려지지 않음 또는 적용할 수 없음
        - 속성 값을 아직 모르거나 해당되는 값이 없음
        - 널 값은 숫자 도메인의 0이나 문자열 도메인의 공백 문자 또는 공백 문자열과 다르다

    6. 차수
        - 한 릴레이션에 들어 있는 속성들의 수
        - 유효한 릴레이션의 최소 차수는 1
        - 릴레이션의 차수는 자주 바뀌지 않는다
    
    7. 카디날리티
        - 릴레이션의 투플 수
        - 유효한 릴레이션은 카디날리티 0을 가질 수 있다
        - 릴레이션의 카디날리티는 시간이 지남에 따라 계속해서 변한다.


### 릴레이션의 구성
    - 릴레이션 스키마
        1. 릴레이션의 논리적 구조, 릴레이션 내포
        2. 릴레이션의 이름과 릴레이션에 포함된 모든 속성 이름으로 정의된다.
        3. 정적인 특징이 있따.

    - 릴레이션 인스턴스
        1. 어느 한 시점에 릴레이션에 존재하는 투플들의 집합, 릴레이션 외연
        2. 동적인 특징이있다.

### 데이터베이스의 구성
    - 데이터베이스 스키마
        1. 데이터베이스의 전체 구조
        2. 데이터베이스를 구성하는 릴레이션 스키마의 모음

    - 데이터베이스 인스턴스
        1. 데이터베이스를 구성하는 릴레이션 인스턴스의 모음


### 릴레이션의 특성
    - 릴레이션은 투플들의 집합이다!
        1. 투플의 유일성
            하나의 릴레이션에는 동일한 투플이 존재할 수 없다 ->키가 존재한다.
        
        2. 속성의 원자성
            속성 값으로 원자값(단일 값)만 사용할 수 있다.
            한 투플의 각 애트리뷰트는 원자 값을 가진다. 



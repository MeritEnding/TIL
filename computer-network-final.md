# 9장

## 전송 계층 서비스
### 프로세스 대 프로세스 통신 
- 전송 계층은 응용 계층과 네트워크 계층사이에 위치하고 있다.
- 이는 로컬호스트와 원격호스트에 존재하는 두 응용계층 사이의 프로세스와 프로세스간 통신을 지원
- 논리적인 연결이다
- 통신을 하려면 종단간의 연결이 필요한데 이것을 프로세스 대 프로세스 통신이라고 한다.

### 포트번호
- 모든 프로세스는 포트번호를 가지고있음
- 클라이언트: 임시포트번호, 서버는: well-known 포트번호를 사용
- 포트번호를 가지고 ip주소와 결합하면 소켓주소(전송계층의 통신을 만들기위한 서버 및 클라이언트의 프로세스 정의)

### 전송층 흐름제어 
- Pushing: 수신자에게 패킷을 먼저 전달하고 수신 버퍼가 꽉 차면 그 때 flow control 해주는거
- Pulling: 수신자가 먼저 송신자에게 요청한만큼만 보내달라고 요청 그러면 송신자는 그 만큼만 보낸다 =>flow control
- 2개의 버퍼 사용: 송신 전송층, 수신 전송층
- 송신 응용계층에서 전송층을 통해 수신측에 메세지를 보내면 메세지를 버퍼에 저장하게된다.
수신측의 응용계층이 버퍼의 메세지를 가져가야하는데 안 가져가면 버퍼가 꽉 찬다. 그래서 송신측 전송층에게 flow control 보내고 송신측 전송층은 응용층에 flow control을 한다.

### 전송층 오류제어

- 훼손된 패킷 감지 및 폐기
- 손실되거나 제거된 패킷을 추적하고 재전송
- 중복 수신된 패킷을 확인하고 폐기
- 손실된 패킷이 도착할때까지 순서에 어긋나게 들어온 패킷을 버퍼에 저장

=> 순서번호가 필요하다
=> ack 확인응답이 필요하다
- 수신측: 훼손된 패킷을 버리고 중복 수신된 패킷을 조용히 폐기한다.
그리고 순서에 어긋나게 들어온 패킷을 폐기(gbn)하거나 손실된 패킷이 도착하기 전까지 버퍼에 저장
- 송신측: ack 확인하려고 타이머를 사용함 타이머가 만료되기전까지 ack 도착하지않으면 송신측은 패킷을 재전송

### 전송층 흐름,오류제어의 결합을 사용
- 송신측 및 수신측 2개의 버퍼 사용, 버퍼에 번호 매겨지고 순서 번호로 사용
- 송신측: 패킷이 전송되면 복사본을 메모리영역에 저장하고 확인응답을 기다림, 확인응답 도착하면 패킷이 제거됨
- 수신측: 순서번호를 가진 패킷이 도착하면 응용층에서 수신할 준비가 될 때까지 메모리에 저장
패킷의 도착을 알리기 위해 확인응답 전송


# 9장 전송 계층 프로토콜
- 단순 프로토콜
    1. 흐름제어나 오류 제어가 없는 단순 비연결형 프로토콜
    2. 수신하면 바로처리한다 => 수신부에서는 과도한 패킷의 수신으로 인한 버퍼 초과가 발생하지 않는다.
    3. 송신부는 수신부를 전혀 고려하지않고 패킷을 차례대로 보낸다.
- 정지-대기 프로토콜
    1. 흐름제어, 오류제어를 사용하며 연결형 프로토콜이다.
    2. 크기가 1인 슬라이딩 윈도우를 사용한다
    3. 한번에 하나의 패킷을 전송=> 다음패킷을 전송하기전에 확인응답을 기다린다
    4. 손상된 패킷을 검출하기위해 검사합을 추가
    5. 검사합을 검사하여 오류가 있으면 패킷 폐기

    - 패킷 손실
        송신부에서 타임아웃되고 패킷 재전송
    - ack 손실
        송신부에서 타임아웃되고 패킷 재전송 => 수신부에서 패킷 버리고 ack 다시보냄
- Go-Back-N 프로토콜
    1. 전송의 효율성을 향상시키기 위해 송신부가 확인응답을 기다리는 동안 다중 패킷들이 전송되어야한다.
    2. 송신부에서 여러 개의 패킷을 전송할 수 있어야한다.
    3. GO-BACK-N 프로토콜에서 확인응답 번호는 누적값이며 수신되기를 기대하는 다음패킷의 순서번호를 의미한다.
    4. 송신 윈도우는 Sf,Sn 그리고 Ssize등 3개의 변수를 가지며 최대값이 2^m - 1 인 버퍼를 나타내는 추상적인 개념
    5. ackNo가 Sf와 Sn사이의 값을 갖는 오류없는 ack를 수신하는 송신 윈도는 하나 이상의 슬롯을 이동한다.
    6. 타이머 전송되는 각각의 패킷에 대해 각각 타이머가 동작
    => 타이머 만료되면 모든 미해결 패킷들은 재전송
    7. 타임아웃이 발생하면 송신측은 n위치만큼 후퇴하여 모든 패킷을 재전송
    송신측에서 이미 패킷 6을 전송하였지만 타이머가 만료될경우, sf=3이면 송신측은 패킷3, 패킷4,패킷5,패킷6까지 4개의 패킷을 재전송한다.
    8. Go-back-n 패킷 손실 =>
    수신측: 어차피 이따가 받을거기때문에 순서에 어긋나게 들어온 패킷을 폐기
    송신측: ack 받지만 확인응답 번호가 sf보다 크지않고 같기때문에 송신부에서 무시되고
    타임아웃 발생하고 n만큼후퇴하고 다시보냄
    ack 손실 =>누적확인응답이기에 송신측은 정상적으로 수행하다 다음 ack가 정상 수신되면
    그 이전까지는 다 잘 처리했구나 판단 

- 선택적 반복 프로토콜
    1. gobackn같은 경우는 잘 전송되었지만 순서에 맞지않다는 이유로 송신부는 모든 패킷들을 재전송한다 => 하부네트워크 프로토콜에서 많은 패킷들이 손실되는 경우 비효율적이다.
    2. 수신측 송신부 윈도 사이즈 같게해서 손실된 패킷만 선택적으로 재전송한다.
    3. 송신 윈도의 최대 크기는 2^m-1이고 수신윈도우는 송신윈도와 크기가 같다.
